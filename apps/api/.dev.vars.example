# 本地开发密钥（.dev.vars，勿提交；上线用 `wrangler secret put` 设置同名 secret）
# 询盘通知邮件的 Resend API 密钥；缺省时询盘只落库，mail_status=skipped
RESEND_API_KEY=
# /api/admin/* 的 Bearer 令牌；这里只是示例占位符，请不要直接使用此值
ADMIN_TOKEN=change-me-to-a-long-random-secret
